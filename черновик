openPopupButtons.forEach( function (button) {
  button.addEventListener('click', function (e) {
    e.preventDefault();
    popup.classList.add('active');
    popupWrapper.classList.add('active');
  })
});

closePopupButton.addEventListener('click', function () {
  popup.classList.remove('active');
  popupWrapper.classList.remove('active');
});

......

let popup = document.querySelector('.popup');
let popupWrapper = document.querySelector('.popup__wrapper');
let openPopupButtons = document.querySelectorAll('.profile__edit-button');
let closePopupButton = document.querySelector('.popup__close');
let profileUsername = document.querySelector('.profile__usermane');
let profileAbout = document.querySelector('.profile__about');
let formElement = document.querySelector('.popup__form');
let usernameInput = formElement.querySelector('.popup__input_username');
let jobInput = formElement.querySelector('.popup__input_job');

openPopupButtons.addEventListener ('click', function () {
  openPopup(popup);
});

function openPopupButtons (modal) {
  modal.classList.add('active')
}

function closePopup(evt) {
const closeBtn = evt.target;

if (closeBtn.classList.contains('popup__close')) {
  closeBtn.closest('.popup').classList.remove ('active');
}
}

//function setPopupInputValue() {
  //usernameInput.value = profileUsername.textContent.trim;
  //jobInputInput.value = profileAbout.textContent.trim;
//}

//function setNodeTextValue() {
  //profileUsername.textContent = usernameInput.value;
  //profileAbout.textContent = jobInputInput.value;
//}

//function handleFormSubmit(evt) {
  //evt.preventDefault();
  //setNodeTextValue();
//}

//popup.addEventListener('click', function () {
  //setPopupInputValue()
  //openPopupButtons(popupWrapper)
//});

//formElement.addEventListener('submit', handleFormSubmit);

.....................


let content = document.querySelector('content');
let profileName = document.querySelector('.profile__usermane');
let profileJob = document.querySelector('.profile__about');
let editButton = document.querySelector('.profile__edit-button');
let popup = document.querySelector('.popup');
let popupForm = document.querySelector('.popup__wrapper');
let nameInput = document.querySelector('.popup__input');
let jobInput = document.querySelector('.popup__input');
let closePopupButton = document.querySelector('.popup__close');

function openPopup() {
  popup.classList.add('popup_opened');
  nameInput.value = profileName.textContent;
  jobInput.value = profileJob.textContent;
}

function closePopup() {
  popup.classList.remove('popup_opened');
}

function handleFormSubmit(event) {
  event.preventDefault();
  profileName.textContent = nameInput.value;
  profileJob.textContent = jobInput.value;
  closePopup();
}

editButton.addEventListener('click', openPopup);
popupForm.addEventListener('submit', handleFormSubmit);
closePopupButton.addEventListener('click', closePopup);